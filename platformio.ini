[platformio]
build_cache_dir = .cache

; This file is for library development only.
; To build and run an example, open its directory as a PlatformIO project:
;   examples/loopback/
;   examples/two_node/
;   examples/walkie_talkie/
;   examples/scope_loopback/
;   examples/bus_monitor/
;
; Each example has its own platformio.ini that references this library
; via lib_extra_dirs = ../..
