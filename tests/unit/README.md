# Unit Tests

Placeholder for future unit tests — no hardware required.

Candidates:
- `calcBitTiming()` — verify rate/SP calculations against known values
- `dlcToLen()` — DLC 0–15 mapping
- EID encode/decode — T0/R0 bit packing
- Filter OBJ/MASK encoding — SID and EID cases
